{
  "name": "sentinel-ui-dev",
  "private": true,
  "type": "module",
  "description": "Development-only harness for ui/index.html. Never shipped: the published page is a single self-contained file, and tests/ui.rs enforces that.",
  "scripts": {
    "start": "node serve.mjs"
  },
  "devDependencies": {
    "agentation": "^3.0.2",
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
